// Shared constants for the DiceBag application
const DIE_TYPES = ["d4", "d6", "d8", "d10", "d12", "d20", "d100"];
const MAX_DICE_PER_TYPE = 50;
const LOW_POOL_THRESHOLD = 10;

module.exports = {
  DIE_TYPES,
  MAX_DICE_PER_TYPE,
  LOW_POOL_THRESHOLD,
};
